<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Edit Chats</title>
    </head>

    <body>
        <h1>Edit Your Chats Here:</h1>
        <p>This chats id is: <%= chat._id %>
        </p>
        <p>This chat was send from <strong>
                <%= chat.from %>
            </strong> to <strong>
                <%= chat.to %>
            </strong></p>
        <form method="post" action="/chats/<%= chat._id %>?_method=PUT">
            <textarea name="msg" id="msg" cols="30" rows="10"><%= chat.msg %></textarea>
            <button>Save Changes</button>
        </form>
    </body>

</html>